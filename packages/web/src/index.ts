// web-client — Web Client
// Scope: Interactive Sudoku SPA that renders a 9x9 board, allows difficulty selection, fetches puzzles from Puzzle Service, validates moves locally, submits results to Scores Service, and displays leaderboard.
// Owns: packages/web
// This team builds its slice here each phase.
export {};

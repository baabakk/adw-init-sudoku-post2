import { Difficulty, Board } from '@init-sudoku-post2/contracts';

/** Supported difficulty levels for puzzle generation. */
export type PuzzleDifficulty = Difficulty;

/** Sudoku board type alias (9x9 matrix). */
export type SudokuBoard = Board;

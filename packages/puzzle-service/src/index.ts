// puzzle-service — Puzzle Service
// Scope: Stateless service that generates uniquely-solvable Sudoku puzzles at three difficulty levels and validates submitted boards. Exposes GET /puzzle and POST /validate endpoints.
// Owns: packages/puzzle-service
// This team builds its slice here each phase.
export {};

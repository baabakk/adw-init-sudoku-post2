// scores-service — Scores Service
// Scope: Service with SQLite persistence that records completed-game results and serves a per-difficulty top-10 leaderboard. Exposes POST /scores and GET /leaderboard endpoints.
// Owns: packages/scores-service
// This team builds its slice here each phase.
export {};
